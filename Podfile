# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'movies-hub' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for movies-hub
  pod 'SDWebImage'
  
  
  target 'movies-hubTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'movies-hubUITests' do
    # Pods for testing
  end
  
end
